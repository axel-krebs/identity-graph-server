<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>eu.tecfox.cloud</groupId>
		<artifactId>igs</artifactId>
		<version>1.0.0</version>
	</parent>

	<artifactId>igs-core-bdb</artifactId>

	<packaging>bundle</packaging>

	<name>IGS :: Core :: Berkley JE</name>

	<description>A fragment that provides persistence to the Core bundle.</description>

	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<berkeleydb.version>3.2.76</berkeleydb.version>
	</properties>

	<dependencies>

		<dependency>
			<groupId>${project.groupId}</groupId>
			<artifactId>igs-core-bundle</artifactId>
			<version>${project.version}</version>
		</dependency>

		<dependency>
			<groupId>berkeleydb</groupId>
			<artifactId>je</artifactId>
			<version>${berkeleydb.version}</version>
		</dependency>

	</dependencies>

	<build>

		<!-- This will end up as 'SymbolicName' in the MANIFEST.MF -->
		<finalName>IGS-Core-Fragment-BDB</finalName>

		<sourceDirectory>src/main/java</sourceDirectory>

		<plugins>

			<plugin>
				<groupId>org.apache.felix</groupId>
				<artifactId>maven-bundle-plugin</artifactId>
				<extensions>true</extensions>
				<configuration>
					<instructions>
						<Bundle-SymbolicName>IGS-Core-BerkeleyDB</Bundle-SymbolicName>
						<Fragment-Host>IGS-Core</Fragment-Host>
						<!-- Nota bene: 'resources' package is NOT exported as OSGI public! Also, a BundleActivator cannot 
							be used here.. -->
						<Private-Package>eu.tecfox.cloud.persistence.bdb,</Private-Package>
						<Export-Package>eu.tecfox.cloud.identity.impl.bdb.*
						</Export-Package>
						<Embed-Dependency>je;scope=runtime</Embed-Dependency>
						<Bundle-Blueprint>OSGI-INF/blueprint/*.xml</Bundle-Blueprint>
					</instructions>
				</configuration>
			</plugin>

		</plugins>

	</build>

</project>
